<%- include('header'); -%>
<body>
    <div class="box" id="heading"> 
        <% if (foo2 === 6 || foo2 === 0){ %>
            <h1 style="color: black;"><%=foo%></h1>
        <% } else { %>
            <h1 style='color:whitesmoke;'><%=foo%></h1>
        <% } %>
    </div>
    
    <div class="box">
        <% for( let index = 0; index < newListItem.length; index++ ) { %>
            <div class="item">
                <input type="checkbox">
                <p><%= newListItem[index] %></p>
            </div>
        <% } %>
        <form class = 'item' action="/" method="POST">
            <input type='text' name='newItem' placeholder="new item" required>
            <button type="submit" name="list" value="<%=foo%>">+</button>
        </form>
    </div>
<%- include('footer'); -%>